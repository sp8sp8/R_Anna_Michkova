#Инициализируйте пустой проект Shiny.
#Добавьте четыре KPI (информационных блока): средние значения по столбцам NA_Sales, EU_Sales, JP_Sales, Other_Sales.


#Постройте линейную диаграмму (динамику), где в качестве «x» будет столбец «Year», а в качестве «y» — четыре линии: суммы по столбцам NA_Sales, EU_Sales, JP_Sales, Other_Sales.
#Постройте столбчатую диаграмму (рейтинг) по жанрам (столбец «Genre»), где в качестве меры будут четыре столбца — также суммы по NA_Sales, EU_Sales, JP_Sales, Other_Sales.
#Создайте таблицу со всеми данными из исходного датасета.
#Создайте фильтр по столбцу «Platform».
#Создайте фильтр по столбцу «Year» с возможностью выбора диапазона годов.
